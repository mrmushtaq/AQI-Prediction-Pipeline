requests>=2.31.0
python-dotenv>=1.0.0
pandas>=2.0.0
pytest>=7.4.0
pytest-mock>=3.11.0
matplotlib
seaborn
scikit-learn
joblib
lightgbm
xgboost
catboost
# Phase 6 (dashboard): Streamlit app, SHAP + LIME model explanations
streamlit
shap
lime
# TensorFlow is not used by the training pipeline in this phase (Phase 5 uses
# tree-based models only); install it separately if a future phase needs it.
# tensorflow

hsfs[python]
confluent-kafka